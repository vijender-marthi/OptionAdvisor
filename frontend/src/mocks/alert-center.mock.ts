import type { AlertCenterPayload, ApiEnvelope } from '../types/commandCenter'

const alerts: AlertCenterPayload['alerts'] = [
  {
    id: 'alert-day-vwap-lost',
    ticker: 'AMD',
    alert_type: 'VWAP_LOST',
    engine_type: 'DAY',
    severity: 'CRITICAL',
    signal: 'EXIT',
    message: 'AMD lost VWAP after the opening push.',
    reason: 'Buyers failed to hold intraday trend support and follow-through faded quickly.',
    recommended_action: 'Exit failed longs and avoid new entries until VWAP is reclaimed with volume.',
    status: 'ACTIVE',
    created_at: new Date(Date.now() - 8 * 60_000).toISOString(),
  },
  {
    id: 'alert-swing-nvda',
    ticker: 'NVDA',
    alert_type: 'SWING_GO',
    engine_type: 'SWING',
    severity: 'INFO',
    signal: 'WATCH',
    message: 'NVDA remains in a healthy swing uptrend.',
    reason: 'Trend structure is still intact and relative strength remains constructive.',
    recommended_action: 'Let run while trend holds',
    status: 'ACTIVE',
    created_at: new Date(Date.now() - 21 * 60_000).toISOString(),
  },
  {
    id: 'alert-regular-avgo',
    ticker: 'AVGO',
    alert_type: 'HIGH_IV_WARNING',
    engine_type: 'REGULAR',
    severity: 'WARNING',
    signal: 'SKIP',
    message: 'AVGO trend is bullish, but option IV is elevated.',
    reason: 'Swing structure is constructive, but option pricing is rich enough to hurt a simple long call entry.',
    recommended_action: 'Use smaller size, longer expiry, or debit spread. Avoid oversized long calls.',
    status: 'ACTIVE',
    created_at: new Date(Date.now() - 16 * 60_000).toISOString(),
  },
  {
    id: 'alert-portfolio-tsla',
    ticker: 'TSLA',
    alert_type: 'SCALE_OUT',
    engine_type: 'PORTFOLIO',
    severity: 'WARNING',
    signal: 'SCALE_OUT',
    message: 'TSLA is up 100% and profits are worth protecting.',
    reason: 'The position has reached an oversized gain relative to normal swing targets.',
    recommended_action: 'Sell 50%, keep runner',
    related_trade_id: 'trade-tsla-runner',
    status: 'ACTIVE',
    created_at: new Date(Date.now() - 12 * 60_000).toISOString(),
  },
  {
    id: 'alert-market-vix',
    ticker: '',
    alert_type: 'VIX_SPIKE',
    engine_type: 'MARKET',
    severity: 'WARNING',
    signal: 'WAIT',
    message: 'VIX is spiking and broad-market risk is rising.',
    reason: 'Elevated volatility often leads to wider spreads, weaker follow-through, and poorer premium selling conditions.',
    recommended_action: 'Reduce size and wait for the tape to stabilize before adding exposure.',
    status: 'ACTIVE',
    created_at: new Date(Date.now() - 5 * 60_000).toISOString(),
  },
]

export const alertCenterEnvelopeMock: ApiEnvelope<AlertCenterPayload> = {
  data: {
    summary: {
      total: alerts.length,
      active: alerts.length,
      critical: alerts.filter(a => a.severity === 'CRITICAL').length,
      warning: alerts.filter(a => a.severity === 'WARNING').length,
      info: alerts.filter(a => a.severity === 'INFO').length,
      profit_protection: alerts.filter(a => ['SCALE_OUT', 'PROTECT_PROFITS', 'PROFIT_TARGET_HIT'].includes(a.alert_type)).length,
      trade_entry: alerts.filter(a =>
        ['DAY_GO', 'DAY_WAIT', 'OR_BREAKOUT', 'SWING_GO', 'SWING_WATCH', 'BREAKOUT_CONFIRMED', 'RELATIVE_STRENGTH', 'LATE_DAY_STRENGTH', 'REGULAR_TRADE', 'REGULAR_WATCH', 'CREDIT_SPREAD_TARGET'].includes(a.alert_type)
      ).length,
    },
    sections: {
      day_trade: alerts.filter(a => a.engine_type === 'DAY'),
      swing_trade: alerts.filter(a => a.engine_type === 'SWING'),
      regular_trade: alerts.filter(a => a.engine_type === 'REGULAR'),
      portfolio: alerts.filter(a => a.engine_type === 'PORTFOLIO'),
      market: alerts.filter(a => a.engine_type === 'MARKET'),
    },
    alerts,
  },
  error: null,
  stale: false,
  fetched_at: new Date().toISOString(),
}
