/** Persisted last Strategy Finder request (TickerPage restore after navigation/refresh). */
export const OA_LAST_OPTION_ANALYSIS_KEY = 'oa_last_option_analysis'

/** Local list of symbols for the Day Trade Watchlist (max 10). */
export const OA_DAY_TRADE_WATCHLIST_KEY = 'oa_day_trade_watchlist_v1'
