# Frontend env (Vite). Copy to `.env.local` for local dev.
#
# Google Sign-In: create an OAuth 2.0 Web client in Google Cloud Console.
# Authorized JavaScript origins: http://localhost:4200 (and your production origin).
# Authorized redirect URIs: not required for the GIS popup flow.
VITE_GOOGLE_CLIENT_ID=
