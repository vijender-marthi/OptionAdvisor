Before coding, read all files in /ai-context. Follow those rules strictly.

## Prompt: Build Trade Command Center Page

The Trade Command Center should provide a unified decision page that aggregates Day, Swing, and Regular trade recommendations. Use the dark trading dashboard style with cards, badges, and expandable rows. The page must request the backend API to retrieve recommendations for all engines and display them in a single view.

- Use React + TypeScript.
- Use Tailwind for styling.
- Keep UI thin; calculations are in the backend.
- Do not duplicate existing business logic.
